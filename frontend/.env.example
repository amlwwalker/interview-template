# Copy to .env.local (Vite loads it automatically and it is gitignored).
#
# Vite only exposes variables prefixed with VITE_ to browser code — a guard
# against leaking server secrets into the bundle.

VITE_API_BASE_URL=http://localhost:8080
